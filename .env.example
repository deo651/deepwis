# AtomForge LLM 配置（全部可选）
#
# ⚠️  安全警告（重要）
# -------------------------------------------------------------------
# 1. `.env` 已在 .gitignore，请勿提交真实 Key 到任何 git 仓库。
# 2. 由于这是纯前端 Vite 工程，`VITE_*` 前缀的环境变量在 `npm run build`
#    时会被直接打包进 `dist/assets/*.js`。这意味着：
#      - 如果把 `dist/` 部署到 Vercel / Netlify 等公开静态托管，
#        访问者可以从 JS 源码里读出 API Key。
#      - 永远不要把启用了真实 Key 的 dist 推送到 public 仓库。
# 3. 想公网部署 + 启用 LLM，请改成「服务端代理」模式（参见 README）：
#    用 Cloudflare Workers / Vercel Functions 承担 Key，前端只调代理。
# 4. 本配置仅适合「本地 dev / 本地 preview」时演示 LLM 接入能力。
#
# OpenAI 兼容接口（Azure OpenAI 也可），形如：
#   VITE_LLM_BASE_URL=https://your-endpoint.openai.azure.com/openai/v1
#   VITE_LLM_MODEL=gpt-4o-mini
#   VITE_LLM_API_KEY=sk-...
#
# 不填或填错时，AtomForge 会自动回退到 DemoProvider（无需 API Key 也能完整体验）。

VITE_LLM_BASE_URL=
VITE_LLM_MODEL=
VITE_LLM_API_KEY=
