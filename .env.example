# AtomForge LLM 配置（全部可选）
#
# 🔐 重要安全设计
# -------------------------------------------------------------------
# 本仓库的 LLM Key **只在服务器进程内存中**：
#   1. Vite plugin `atomforge-llm-proxy` (见 vite.config.ts) 在 dev / preview 启动时
#      用 loadEnv 读取本文件，存入闭包变量。
#   2. 服务器注册同源 `/api/llm/status` 与 `/api/llm/chat`，前端只调代理。
#   3. **浏览器永远不接触 Key**；dist bundle 中也不会出现 Key 字符串。
#
# 因此变量名 **不带 VITE_ 前缀**，确保它绝不会被 Vite 注入到 import.meta.env。
# 旧的 VITE_LLM_* 变量名仍然兼容读取，但请尽快迁移到 LLM_*。
#
# 这意味着：
#   - 本地 dev / preview 可以安全启用真实 LLM
#   - 公网部署也可以启用 LLM（前提：服务器配 .env，反代到 5173）
#   - 把 dist 单独丢到 Vercel/Netlify/CloudFront 等纯静态托管时，
#     这些平台不会执行 vite preview，因此 /api/llm/* 不存在，
#     前端会自动只跑 DemoProvider —— 仍然合规且可演示。
#
# OpenAI 兼容接口（Azure OpenAI 也可），形如：
#   LLM_BASE_URL=https://your-endpoint.openai.azure.com/openai/v1
#   LLM_MODEL=gpt-4o-mini
#   LLM_API_KEY=sk-...
#
# 不填或填错时，AtomForge 会自动回退到 DemoProvider（无需 API Key 也能完整体验）。
#
# ⚠️ 永远不要把含有真实 Key 的 .env 提交到任何 git 仓库。

LLM_BASE_URL=
LLM_MODEL=
LLM_API_KEY=
